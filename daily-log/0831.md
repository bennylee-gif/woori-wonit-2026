`display: flex`를 부모 상자에 걸면, 그 안의 자식들이 **서랍 속 물건처럼 한 줄로** 정렬됩니다.
`justify-content` : 가로축 정렬
`space-between` → 양 끝으로 밀어 배치 (버튼 두 개를 좌우로)
`center` : 가로 가운데 정렬
`align-items: center` → 세로 가운데 정렬
`gap: 8px` → 요소 사이 간격x

- ctlr + /를 동시에 누르면 주석을 지정/해제

**flex 관련 정리본**
https://studiomeal.com/archives/197/

- 채팅창에 @vscode 라고 치면 vscode에 질문하기 가능
- ctrl + shift + p를 열고 Quokka 실행

## 자바스크립트
/*
여러줄
주석
*/
// 한 줄 주석

- const(기본값) : 한 번 만들면 값을 바꾸는 게 불가능한 방
- let : 한 번 만들면 값을 바꾸는 건 가능하지만, 지우고 새로 만들 수 없는 방
- var : 매번 만들고 지울 수 있는 방(사용 X; 사이드 이펙트 문제)

## 변수 명명 규칙
- `camelCase`(기본) : 소문자로 시작하고, 띄어쓰기 자리마다 대문자로 처리하는 이름 규칙
- `PascalCase` : 대문자로 시작하고, 띄어쓰기 자리마다 대문자로 처리하는 이름 규칙(Class 이름 같은 변수들 보다 더 큰 개념을 담는 변수명에 주로 사용)
- `snake_case` : 소문자로 시작하고, 띄어쓰기 자리마다 _(언더바)로 처리하는 이름 규칙
- `kebab-case` : 소문자로 시작하고, 띄어쓰기 자리마다 -(dash)로 처리하는 이름 규칙
<br/>
- class를 만들때는 PascalCase를 씁니다.
- 숫자, _, $을 변수명에 사용 가능합니다. 근데 숫자는 맨 앞에는 들어올 수 없습니다.
- 공백, 마침표, 자바스크립트가 이미 쓰는 keyword(예약어)는 불가합니다.

## 자료형
- number(숫자) : 정수, 실수를 모두 number
- string(문자열)
- boolean: true, false로 반환
- `null`: 의도적으로 "비어있음"을 명시할 때 직접 할당. 값이 없음. 자료형은 object로 찍힘. 웬만하면 우리가 작성하는 빈방에는 null로 채웁니다.
- undefined: 값을 할당되지 않은 변수 / 리턴이 없는 함수의 실행 결과 / 존재하지 않는 속성을 가리킬 때 자바스크립트가 돌려주는 결과
- symbol: 고유하고 변경할 수 없는 값. 
- bigInt : 아주 큰 숫자. 숫자 뒤에 소문자 n을 붙여서 관리
- object: 객체
- 배열, 날짜, 함수, Map, Set, 정규식
<br/>
- `글자 ${보여지는 값} 글자` : `(벡터)

내가 읽기 쉬운 코드보다 동료가 읽었을 때 한눈에 의도가 파악되는 코드가 좋은 코드
- ()로 우선순위 구분
- 증감 연산자 (++, --): "단독 문장으로만 사용"
- console.log(3 ** 2)  //제곱

## 비교연산자
- = (대입연산자) var a = 1;
- ~~== (항등연산자, 동등성 비교): 값이 같은지(버그의 원인)~~
- **=== (완전항등연산자, 동일성 비교): 자료형, 값까지 비교**
~~다른 언어와 구분되는 부분!, 손에 익히는 것을 권장~~

- 논리연산자 &&(and), ||(or), !(not) 

- 조건문 : if ~ else if ~ else / switch ~ case 문이 존재합니다. 

swtich (명제) {
    case (1) : 조건 만족시 실행문
               break;
    case (2) : 조건 만족시 실행문
               break;
    default: 위의 모든 조건을 만족하지 못할 경우 실행문 
              break; // 마지막이면 안 걸어도 됩니다.
}

- 복합연산자 && (and) / || (or) / ! (not)으로 여러개 조건을 한번에 비교하기
- 삼항연산자 - 불표현식 ? 참 : 거짓